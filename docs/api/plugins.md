# Plugins

Plugin-specific integrations are maintained in separate repositories. This library provides the core primitives (`MCPServer`, `MCPClient`, `@tool`) to build your own integrations.

See `AGENTS.md` in the repository root for how-to guides on adding MCP servers and agent-as-a-tool patterns.
